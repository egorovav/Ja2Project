<Window x:Class="WpfTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:CommonWpfControls;assembly=CommonWpfControls"
        xmlns:local="clr-namespace:WpfTest"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions >
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <TextBox Text="{Binding ElementName=fev, Path=FlagsString, Mode=OneWay}" />
        <controls:FlagEditorView x:Name="fev" Grid.Row="1"/>
        <DataGrid x:Name="dg" Grid.Row="2" 
                  CanUserAddRows="True" 
                  CanUserDeleteRows="True"
                  AutoGenerateColumns="False"
                  ItemsSource="{Binding Emploees}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Path=Name}" />
                <DataGridTextColumn Header="SurName" Binding="{Binding Path=SurName}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Image Grid.Column="1" Source="Images/ClockwiseRotateArrow.bmp" />
        <local:TestUserControl Grid.Column="1" Grid.Row="1" />
    </Grid>
</Window>
