<Window x:Class="HdsJsdConverter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Convert JSD To JSD-HD." Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Menu>
            <MenuItem Header="File">
                <MenuItem Header="Open _File" Click="OpenFile_Click" />
                <MenuItem Header="Open _Directory" Click="OpenDirectory_Click" />
            </MenuItem>
        </Menu>
        <ScrollViewer Grid.Row="1">
            <TextBlock x:Name="tb" Margin="5" />
        </ScrollViewer>
        <Button Content="Convert" Grid.Row="2" Width="100" Height="40" Margin="5" Click="btnConvert_Click" />
    </Grid>
</Window>
