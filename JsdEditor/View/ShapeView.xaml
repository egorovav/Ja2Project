<UserControl x:Class="JsdEditor.ShapeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JsdEditor"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             Loaded="UserControl_Loaded">
    <Border BorderThickness="1" BorderBrush="LightGray" Padding="2,0,2,0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <StackPanel x:Name="sp">
                <UniformGrid x:Name="ugShapeValue" Margin="2,0,2,0" />
            </StackPanel>
            <UniformGrid x:Name="ugLayers" Grid.Row="1">

            </UniformGrid>
        </Grid>
    </Border>
</UserControl>
